# Server port
PORT=8080

# SQLite database path
DB_PATH=/data/db/bedrock.db

# JWT signing secret (auto-generated if empty)
JWT_SECRET=

# Initial admin credentials
INIT_USERNAME=admin
INIT_PASSWORD=admin123
